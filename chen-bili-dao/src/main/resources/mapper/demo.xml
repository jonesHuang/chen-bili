<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chen.bili.dao.DemoDao">
    <select id="queryId" parameterType="java.lang.Long" resultType="java.lang.Long">
        select * from t_demo where id =#{id}
    </select>
</mapper>